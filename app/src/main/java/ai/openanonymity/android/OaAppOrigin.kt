package ai.openanonymity.android

object OaAppOrigin {
    const val SCHEME = "https"
    const val HOST = "chat.openanonymity.ai"
    const val ROOT_URL = "$SCHEME://$HOST/"
    const val ORIGIN_RULE = "$SCHEME://$HOST"
}
